# Godot 4 editor cache — regenerated on open
.godot/

# Exported builds
/export/
/build/
export_presets.cfg

# OS cruft
.DS_Store
Thumbs.db

# Promo assets + capture tooling — not part of the template
marketing/
.capture/
tools/capture.gd
tools/capture.gd.uid
tools/capture.tscn
store-listing.md

# The editor rewrites project.godot on every open (engine floor, uid paths).
# project.godot.master is the canonical one and is what the build ships.
